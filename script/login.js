  form.addEventListener('submit',function(event){
    event.preventDefault()
    
  if(true){
    var email = document.getElementById('email').value;
    var password = document.getElementById('password').value;
    var email = localStorage.find(email)
    
  }
})